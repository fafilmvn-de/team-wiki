# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Runtime secrets (passcode + bearer token — see ADR 0011)
.passcode
.session

# Local-only backups generated by serve_admin.py + build_semantic.py
*.bak
inventory.json.bak
semantic/semantic.json.bak
semantic/semantic.xlsx.bak
semantic/backup/

# Optional: ignore generated artefacts if you prefer to commit only sources.
# Default: keep them tracked so consumers can browse the wiki straight from
# `git clone`. Uncomment to flip to source-only repo mode.
# /index.html
# /inventory.xlsx
# /semantic/08_Semantic_Model.html

# Playwright (uat_admin.py)
playwright-report/
test-results/
